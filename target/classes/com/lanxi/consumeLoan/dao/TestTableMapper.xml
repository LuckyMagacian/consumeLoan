<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.lanxi.consumeLoan.dao.TestTableDao">
	<insert id="addTestTable" parameterType="com.lanxi.consumeLoan.entity.TestTable">
		insert into test_table
		(id)
		values
		(
			#{id}
		)
	</insert>
	<delete id="deleteTestTableByClass" parameterType="com.lanxi.consumeLoan.entity.TestTable">
		delete from test_table
		<where>
			<if test="id != null"> and id = #{id}  </if>
		</where>
	</delete>
	<update id="updateTestTableByClass">
		update test_table
		<set>
			id=#{testTable.id}
		</set>
		<where>
			<if test="param.id != null"> AND id = #{param.id}</if>
		</where>
	</update>
	<select id="selectTestTableByClass" resultMap="testTableMap"  resultType="com.lanxi.consumeLoan.entity.TestTable" parameterType="com.lanxi.consumeLoan.entity.TestTable">
		select
			id
		from test_table
		<where>
			<if test="id != null"> AND id = #{id}</if>
		</where>
	</select>
	<resultMap id="testTableMap" type="com.lanxi.consumeLoan.entity.TestTable">
		<result property="id" 			column="id"></result>
	</resultMap>
</mapper>
